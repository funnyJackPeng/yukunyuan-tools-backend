# port
server:
  port: ${PORT:8080}
  error:
    include-message: always
    include-exception: true
    include-binding-errors: always
  # basePath
  servlet:
    context-path: /api

spring:
  application:
    name: email
  datasource:
    url: jdbc:postgresql://pgm-bp1l4kw000kv8343fo.rwlb.rds.aliyuncs.com:5432/yukunyuan_tools?useUnicode=true&characterEncoding=UTF-8
    username: postgres
    password: sS554517318!
